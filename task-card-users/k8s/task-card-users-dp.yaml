apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-card-users-dp
  namespace: task-card
spec:
  selector:
    matchLabels:
      app: task-card-users
  revisionHistoryLimit: 0
  template:
    metadata:
      labels:
        app: task-card-users
        #tier: backend
        #service: users
    spec:
      containers:
      - name: task-card-users-container
        image: luisavelino/task-card-users:1.0
        imagePullPolicy: Always
        envFrom:
        #- secretRef:
        #    name: task-card-users-sec
        - configMapRef:
            name: task-card-users-cfg